# Assets

| File | Used by | Source |
| --- | --- | --- |
| `icon.png` | apple-touch-icon, `og:image` | copy of `src-tauri/icons/icon.png` (512 × 512) |
| `icon-256.png` | favicon, nav wordmark, footer mark | the same icon, downscaled to 256 × 256 |

Both are copies of the app icon, so regenerating the Tauri icons means recopying them here.
Everything else on the page — the home screen and the editor — is markup, not a screenshot.
